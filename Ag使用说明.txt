
2019-11-30
1、填加Ag文件夹
	用于自己的Scene使用
2、填加assets文件夹
	用于调用使用的.svg图
3、constants.py
	FILE_DIR = ""

2019-12-04
4、新建NONeed文件夹，放入不需要的文件

2019-12-05
5、extract_scene.py
    #用于检查字符串是否是以指定子字符串开头，如果是则返回 True，否则返回 False。
    if not obj.__module__.startswith(module.__name__):
        return False

2019-12-06
6、使用github管理